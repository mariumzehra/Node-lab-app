version: "3.9"
services:
  app:
    build: .
    container_name: node-lab-app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
